# Deliberately wrong, to show what validation says before anything is run.
# Not part of the green suite: run it with --test suites/broken.yaml
name: catches a typo before the network
steps:
  - id: whoops
    type: htpp
    url: /posts/1
assert:
  - type: statuss
    expected: 200
